import React, { Component } from 'react';
import {
  View,
  StyleSheet,
} from 'react-native';

class Home extends Component {
  render() {
    return <View>
    </View>
  }
}

Home.propTypes = {
};

const styles = StyleSheet.create({
});

export default Home;
